import React from 'react';
import classes from './Footer.module.css';

const footer = () => {
    return (
        <footer className={classes.container}>
            <h6 className={classes.title}>This is the footer</h6>
        </footer>
    );

}

export default footer;